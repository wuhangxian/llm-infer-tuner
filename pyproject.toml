[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "llm-infer-tuner"
version = "0.1.0"
description = "LLM inference parameter optimization planning agent"
requires-python = ">=3.11"
dependencies = [
  "packaging>=24.0",
  "pydantic>=2.7",
  "pyyaml>=6.0",
  "typer>=0.12"
]

[tool.hatch.build.targets.wheel]
packages = ["planner", "schemas", "runners"]

[dependency-groups]
dev = [
  "pyright>=1.1.350",
  "pytest>=8.0",
  "ruff>=0.5.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.pyright]
# Keep the CI type gate focused on shipped code.  The test suite intentionally
# uses lightweight structural fakes (rather than production Remote/Container
# subclasses), so checking it with Pyright's basic mode produces noise without
# improving the contract enforced by pytest.
include = ["planner", "schemas", "runners"]
exclude = [".venv", "tests"]
pythonVersion = "3.11"
typeCheckingMode = "basic"
venvPath = "."
venv = ".venv"
